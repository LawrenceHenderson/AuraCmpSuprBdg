<aura:component controller="BoatSearchFormApexController" implements="force:appHostable, flexipage:availableForAllPageTypes">
    <!--
<aura:component controller="BoatSearchFormController" implements="force:appHostable">
    controller="BoatSearchFormController"
        implements="flexipage:availableForAllPageTypes"

    <aura:attribute name="boatSelList" type="String[]"/>
    
    <aura:attribute name="selectedBoatType" type="String"/>;

    <aura:attribute name="newBoat" type="Boat__c" default="{
        'sobjectType':'Boat__c',
        'Name':'',
        'Contact__c':'',
        'BoatType__c':'',
        'Description__c':'',
        'Geolocation__Latitude__s':'',
        'Geolocation__Longitude__s':'',
        'Length__c':0,
        'Picture__c':'',
        'Price__c':0,
        'Year_Built__c':0 }" />
-->
        <!-- <aura:registerEvent name="addBoatEvt" type="c:boatSearchEvent"/> -->

        <aura:attribute name="BoatTypes" type="BoatType__c[]" />     
        <!-- <aura:attribute name="teams" type="Boolean" /> -->
        <aura:attribute name="teams" type="Boolean" default="false"/>     
    
    <!--
    <aura:attribute name="selectedBoatType" type="BoatType__c" default="{
            'sobjectType':'BoatType__c',
            'Name':'' }"/>
-->
    <!-- Handle component init in a client-side controller -->
    <aura:handler name="init" value="{!this}" action="{!c.doInit}"/>

    <!-- <lightning:card title="Find a Boat"> -->
        <div class="slds-p-around_medium">
                <lightning:layout horizontalAlign="center" verticalAlign="end">
                    <lightning:layoutitem padding="horizontal-medium">
                        <lightning:select aura:id="boatTypes" name="fab" label="" required="false">
                            <!--
                            <option value="" selected="true"></option>
                            <option>Ski Boat</option>
                            <lightning:select aura:id="boatTypes" label="" name="selectType"
                            onchange="{!c.handleChange}">
-->
                            <option value="">All Types</option>
                            <aura:iteration items="{!v.BoatTypes}" var="boatType">
                                <option value="{!boatType.Id}" text="{!boatType.Name}"/>
                            </aura:iteration>
                        </lightning:select>
                    </lightning:layoutitem>
                    <lightning:layoutitem padding="horizontal-medium">
                        <lightning:button class="slds-button" variant="brand" label="Search" />
                        <aura:if isTrue="{!v.teams}">
                            <lightning:button class="slds-button" variant="brand-outline" label="New"  onclick="{!c.addNewBoat}"/>
                        </aura:if>                     
                    </lightning:layoutitem>
                </lightning:layout>
        </div>
    <!-- </lightning:card> -->

</aura:component>	
